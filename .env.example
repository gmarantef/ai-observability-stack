# ── Tracing stack (ClickHouse + OpenLIT) ─────────────────────────────────────
OPENLIT_DB_USER=default
OPENLIT_DB_PASSWORD=OPENLIT
OPENLIT_DB_NAME=openlit

# ── LiteLLM proxy — API keys para providers remotos ──────────────────────────
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
